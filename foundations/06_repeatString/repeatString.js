const repeatString = function() {
 if (num < 0)
    {
        return "ERROR";
    }

    else
    {
        let word = "";

        for (let i = 0; i < 3; i++)
        {
            word += string;
        }
        return word;
    }
};

// Do not edit below this line
module.exports = repeatString;
